package com.insa.swim.orchestrator.configuration;


/**
 *
 * @author Alex
 * 
 * it is a temporay solution
 * TODO : use a file or directly in the scenario
 */
public class ConsumerLocation {
    /*
    private static final ArrayList<ScenarioToSend> consumers = new ArrayList<ScenarioToSend>();
    
    private ConsumerLocation() {
        ScenarioToSend cons = new ScenarioToSend();
        cons.name = "consumer1";
        cons.id = 1;
        cons.address = "127.0.0.1:8080/ConsumerWS1";
        consumers.add(cons);
        
        cons.name = "consumer2";
        cons.id = 2;
        cons.address = "127.0.0.1:8080/ConsumerWS2";
        consumers.add(cons);
        
        cons.name = "consumer3";
        cons.id = 3;
        cons.address = "127.0.0.1:8080/ConsumerWS3";
        consumers.add(cons);
    }
    
    public String getAddress(int id) {
        for (ScenarioToSend c : consumers) {
            if (c.id == id) {
                return c.address;
            }
        }
        return null;
    }
    
    public String getAddress(String name) {
        for (ScenarioToSend c : consumers) {
            if (c.name.equals(name)) {
                return c.address;
            }
        }
        return null;
    }
    
*/
}
